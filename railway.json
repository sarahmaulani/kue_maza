{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && gunicorn spk_maza.wsgi:application --workers 1 --threads 1 --timeout 120 --bind 0.0.0.0:$PORT"
  }
}
