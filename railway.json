{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && python auto_create_admin.py && python manage.py collectstatic --noinput && gunicorn spk_maza.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
